import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrap: {
    width: '100%',
    backgroundColor: '#a9c2ff',
    padding: 15,
    borderRadius: 10,
    alignItems: 'center',
    marginBottom: 20,
  },
  text: {
    color: '#fff',
    fontSize: 30,
  },
});
